<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:helpers="clr-namespace:TM.Framework.Common.Helpers.UI"
                    xmlns:layout="clr-namespace:TM.Framework.Common.Controls.Layout">

    <ResourceDictionary.MergedDictionaries>

        <ResourceDictionary Source="StandardMargins.xaml"/>
        <ResourceDictionary Source="StandardGeometry.xaml"/>
        <ResourceDictionary Source="ButtonStyles/StandardButtons.xaml"/>
        <ResourceDictionary Source="TextStyles/StandardText.xaml"/>
        <ResourceDictionary Source="InputStyles/StandardInputs.xaml"/>
        <ResourceDictionary Source="CardStyles/StandardCards.xaml"/>
        <ResourceDictionary Source="ListBoxStyles/StandardListBox.xaml"/>
        <ResourceDictionary Source="ScrollbarStyles/StandardScrollbar.xaml"/>
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="Control">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
        <Setter Property="Height" Value="{DynamicResource StandardButtonHeight}"/>
        <Setter Property="MinWidth" Value="{DynamicResource SmallButtonWidth}"/>

        <Setter Property="helpers:SmartButtonHelper.EnableSmartStyle" Value="True"/>
    </Style>

    <Style TargetType="TextBlock" BasedOn="{StaticResource BodyTextStyle}">

    </Style>

    <Style TargetType="TextBox" BasedOn="{StaticResource StandardTextBoxStyle}">
        <Setter Property="helpers:TextInputContextMenuHelper.EnableStandardEditMenu" Value="True"/>
    </Style>

    <Style TargetType="PasswordBox" BasedOn="{StaticResource StandardPasswordBoxStyle}">
        <Setter Property="helpers:TextInputContextMenuHelper.EnableStandardEditMenu" Value="True"/>
    </Style>

    <Style TargetType="RichTextBox">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="helpers:TextInputContextMenuHelper.EnableStandardEditMenu" Value="True"/>
    </Style>

    <Style TargetType="ComboBox" BasedOn="{StaticResource StandardComboBoxStyle}"/>

    <Style TargetType="GroupBox" BasedOn="{StaticResource StandardGroupBoxStyle}"/>

    <Style TargetType="layout:IconHeaderGroupBox" BasedOn="{StaticResource StandardGroupBoxStyle}"/>

    <Style TargetType="ScrollBar" BasedOn="{StaticResource StandardScrollBarStyle}"/>

    <Style TargetType="ScrollViewer" BasedOn="{StaticResource StandardScrollViewerStyle}"/>

    <Style TargetType="ListBox" BasedOn="{StaticResource CardListBoxStyle}"/>

    <Style TargetType="DataGrid">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="RowBackground" Value="Transparent"/>
        <Setter Property="AlternatingRowBackground" Value="Transparent"/>
        <Setter Property="HorizontalGridLinesBrush" Value="Transparent"/>
        <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource SurfaceGlass}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextSecondary}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="FontSize" Value="13"/>
    </Style>

    <Style TargetType="DataGridRow">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource SelectedBackground}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="DataGridCell">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="10,0"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Style.Triggers>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="Transparent"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="TabControl">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>

    <Style TargetType="TabItem">
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="Padding" Value="12,6"/>
    </Style>

    <Style TargetType="ContextMenu">
        <Setter Property="Background" Value="{DynamicResource DialogGlass}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource GlassBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="4"/>
        <Setter Property="helpers:RefreshEffectHelper.EnableRefreshEffect" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContextMenu">
                    <Border Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <StackPanel IsItemsHost="True"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="MenuItem">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
        <Setter Property="Height" Value="32"/>
        <Setter Property="Padding" Value="8,4"/>
        <Setter Property="Opacity" Value="1"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="MenuItem">
                    <Border x:Name="Border"
                            Background="Transparent"
                            BorderThickness="0"
                            CornerRadius="6"
                            Margin="2">
                        <ContentPresenter ContentSource="Header"
                                        VerticalAlignment="Center"
                                        Margin="{TemplateBinding Padding}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Border" Property="Background" Value="{DynamicResource HoverBackground}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="{DynamicResource TextDisabled}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Separator">
        <Setter Property="Background" Value="{DynamicResource GlassBorderBrush}"/>
        <Setter Property="Height" Value="1"/>
        <Setter Property="Margin" Value="4,2"/>
    </Style>

    <Style TargetType="ToolTip">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToolTip">

                    <Grid>

                        <Border Background="{DynamicResource SurfaceGlass}"
                                CornerRadius="6"
                                Margin="4">
                            <Border.Effect>
                                <DropShadowEffect ShadowDepth="1" BlurRadius="6" Opacity="0.2" Direction="270" RenderingBias="Performance"/>
                            </Border.Effect>
                        </Border>

                        <Border Background="{DynamicResource SurfaceGlass}"
                                BorderBrush="{DynamicResource GlassBorderBrush}"
                                BorderThickness="1"
                                CornerRadius="6"
                                Padding="10,6"
                                Margin="4">
                            <ContentPresenter/>
                        </Border>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>

